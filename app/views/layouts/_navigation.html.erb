<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">      <%= image_tag "fpx20pct.png"%></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <%= link_to 'Overview', root_path, class: 'navbar-brand' %>
        <%= link_to 'Metrics', emails_total_path, class: 'navbar-brand' %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Data <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Upload emails domain records",  emails_upload_path %> </li>
            <li><%= link_to "Classify email domain bridge",  emails_classify_path %> </li>
          </ul>
        </li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><% if current_user %>Account<%else%> Please login. <%end%> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% if current_user %>
                    <li>Hello <%= current_user.first_name %></li>
                    <li><%= link_to "Logout", session_path(current_user), :method=>:delete %></li>
                  <% else %>
                    <li><a href="#" class="login">Login</a></li>
                    <div class="login_form">
                    <%= form_tag sessions_path do %>
                    <div class="field">
                      <%= label_tag :email %><br>
                      <%= text_field_tag :email, params[:email] %>
                      </div>
                      <div class="field">
                        <%= label_tag :password %><br>
                        <%= password_field_tag :password %>
                      </div>
                      <div class="field actions"><%= submit_tag "Log in" %></div>
                      <% end %>
                    </div>
                    <% end %>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
